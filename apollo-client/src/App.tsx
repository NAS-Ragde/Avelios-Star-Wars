import './components/worktable/Worktable';
import './App.css'
import Worktable from "./components/worktable/Worktable";

function App() {
    return (
        <div className={'App'}>
            <header className={'App-header'}>
                <Worktable/>
            </header>
        </div>
    );
}

export default App;
